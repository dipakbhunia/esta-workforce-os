import EntityListPage from '@/pages/EntityListPage';

export default function LeaveTypesPage() {
  return <EntityListPage title="Leave Types" entity="Leave Type" description="Define leave categories, defaults, and approval behavior." />;
}
