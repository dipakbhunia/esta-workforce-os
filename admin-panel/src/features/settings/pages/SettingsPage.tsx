import EntityListPage from '@/pages/EntityListPage';

export default function SettingsPage() {
  return <EntityListPage title="Settings" entity="Setting" description="Configure workspace preferences, integrations, and admin controls." />;
}
