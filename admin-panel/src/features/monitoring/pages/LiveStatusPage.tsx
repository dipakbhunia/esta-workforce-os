import EntityListPage from '@/pages/EntityListPage';

export default function LiveStatusPage() {
  return <EntityListPage title="Live Status" entity="Live Status" description="Monitor normalized employee working, break, away, and offline states." />;
}
